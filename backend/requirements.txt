# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Config & validation
pydantic[email]==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Database + pgvector
SQLAlchemy==2.0.37
psycopg2-binary==2.9.10
pgvector==0.3.6

# Embeddings (fully local / on-prem)
sentence-transformers==3.3.1

# Document parsing
PyMuPDF==1.25.2
python-docx==1.1.2

# Chunking
langchain-text-splitters==0.3.4

# Ollama / HTTP client
httpx==0.28.1

# Auth (JWT + Google OAuth)
python-jose[cryptography]==3.3.0
authlib==1.4.0
itsdangerous==2.2.0
# authlib uses httpx (declared above) for the OpenID discovery + token exchange.

# Testing
pytest==8.3.4
